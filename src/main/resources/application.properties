spring.application.name=AluraCallengeForo
spring.datasource.url=jdbc:mysql://${DB_HOST}/${DB_FORO}
spring.datasource.username=${MYSQL_SERVER_USER}
spring.datasource.password=${MYSQL_SERVER_ROOT}

spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

api.security.secret=${JWT_SECRET}
